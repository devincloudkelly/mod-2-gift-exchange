<h1> <%= @user.name %>'s Wishlist </h1>
<ul>
<% @wishlist.each do |wish| %>
<li><%= wish.item_name %></li>
</ul>
<% end %>

<% prev_event = Event.find(params[:event_id])%>
<h3><%= link_to "Return to #{prev_event.name} Gift Exchange", "/events/#{params[:event_id]}" %></h3>